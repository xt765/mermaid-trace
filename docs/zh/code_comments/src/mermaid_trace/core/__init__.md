# 文件: src/mermaid_trace/core/__init__.py

## 概览
此文件是 `mermaid_trace.core` 包的初始化文件。它通过导出核心组件（如 `trace`、`config` 和 `LogContext`）来简化内部引用。

## 源代码与中文注释

```python
"""
MermaidTrace 核心逻辑包。
包含装饰器、上下文管理、配置和事件定义。
"""

# 此文件通常为空，或仅用于导出核心组件以简化包内导入路径。
```
