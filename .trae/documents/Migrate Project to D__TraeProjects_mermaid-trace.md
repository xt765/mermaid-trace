收到，我已确认 `D:` 盘可用。以下是将项目迁移至 `D:\TraeProjects\mermaid-trace` 的详细中文执行计划：

### 执行计划

1.  **准备目标目录**：
    *   检查并创建 `D:\TraeProjects` 目录（如果尚不存在）。

2.  **迁移项目文件**：
    *   使用 `robocopy` 命令将当前项目的所有文件复制到新位置 `D:\TraeProjects\mermaid-trace`。
    *   **排除项**：我会自动排除 `.venv`（虚拟环境）、`__pycache__`、`build` 和 `dist` 等临时文件，确保新环境干净整洁。
    *   **保留项**：`.git` 版本控制记录会被完整保留，您的提交历史不会丢失。

3.  **验证迁移结果**：
    *   检查新目录下是否存在 `pyproject.toml` 等关键文件，确保复制无误。

4.  **完成通知**：
    *   迁移完成后，我会通知您。**注意**：迁移后，建议您在 IDE 中打开新的文件夹 `D:\TraeProjects\mermaid-trace`，并重新创建虚拟环境。

**是否开始执行迁移？**