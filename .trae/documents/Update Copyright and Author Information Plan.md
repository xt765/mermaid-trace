收到，我将使用 `xt765@foxmail.com` 作为作者邮箱。

以下是更新后的执行计划：

### 1. 更新项目配置 (`pyproject.toml`)
*   **作者信息**: 将 `{ name = "MermaidTrace Team", email = "team@mermaidtrace.dev" }` 更新为 `{ name = "xt765", email = "xt765@foxmail.com" }`。

### 2. 更新许可证文件 (`LICENSE`, `docs/*/LICENSE`)
*   **英文许可证**: 将 `Copyright (c) 2026 MermaidTrace Team` 替换为 `Copyright (c) 2026 xt765`。
*   **中文许可证**: 将 `版权所有 (c) 2026 MermaidTrace 团队` 替换为 `版权所有 (c) 2026 xt765`。

### 3. 源代码与文档检查
*   搜索并替换所有文件中可能残留的 `MermaidTrace Team` 或 `team@mermaidtrace.dev` 字符串。

### 4. 验证
*   运行构建和测试命令以确保一切正常。

**准备执行更新？**